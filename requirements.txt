# on MacOS replace apt-get by brew, remove -y option

apt-get install -y git
apt-get install -y wget
#brew install wget
#brew install git

conda create -n lasif python=2.7
source activate lasif
conda install numpy jupyterlab numexpr matplotlib basemap itertools mpi4py
conda install -c conda-forge basemap-data-hires
conda install -c obspy obspy=1.1.0
conda install -c conda-forge instaseis
pip install https://github.com/krischer/wfs_input_generator/archive/master.zip 
pip install geographiclib progressbar colorama joblib flask flask-cache geojson pytest mock nose flake8
pip install pyproj==1.9.6 Pillow

apt-get install -y python-qt4
#brew install python-qt4

pip install pyqtgraph flask-caching

git clone https://github.com/AmSergeant/LASIF_specfem3D.git
cd LASIF_specfem3D
pip install -v -e .


# instaseis 1.3.0 has requirement obspy>=1.1.0
# basemap-data-hires is for my modif to plot_stations
# for basemap-data-hires, ask me to update conda
conda update -n base -c defaults conda
